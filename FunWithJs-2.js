console.log(20+"1") //201
console.log(20-"1") //19

// + (Addition/Concatenation): When one of the operands is a string, JavaScript defaults to string concatenation.
// - (Subtraction): JavaScript interprets the operation as numeric, so it converts both operands to numbers if necessary.
// This behavior is a result of JavaScript's loose typing and automatic type coercion, which can sometimes lead to unexpected results.